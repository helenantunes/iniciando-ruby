a = 2
puts a
a += 4 # é a mesma coisa que (a = a + 4) so que reduzido
puts a
a -= 4 # é a mesma coisa que (a = a - 4) so que reduzido 
puts a
a *= 4 # é a mesma coisa que (a = a * 4) so que reduzido 
puts a
a /= 4 # é a mesma coisa que (a = a / 4) so que reduzido 
puts a
a %= 4 # é a mesma coisa que (a = a % 4) so que reduzido 
puts a
a **= 4 # é a mesma coisa que (a = a ** 4) so que reduzido 
puts a